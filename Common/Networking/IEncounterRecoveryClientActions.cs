namespace Convergence.Common.Networking;

// Optional definition-scoped capability of the shared, reusable recovery item.
internal interface IEncounterRecoveryClientActions
{
    void RequestReviveNearest();
}
